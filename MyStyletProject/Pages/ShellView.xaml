<Window x:Class="MyStyletProject.Pages.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:s="https://github.com/canton7/Stylet"
        xmlns:local="clr-namespace:MyStyletProject.Pages"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance local:ShellViewModel}"
        Title="Stylet Project" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="80"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" FontSize="30" HorizontalAlignment="Center" Text="{Binding text, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBox Grid.Row="1" Text="{Binding text, UpdateSourceTrigger=PropertyChanged}" Width="300"/>
        <Button Grid.Row="2" Content="Click me" Width="100" Height="100" Command="{s:Action ClickedCommand}"/>
        <TextBlock Grid.Row="3" FontSize="30" HorizontalAlignment="Center" Text="{Binding after, UpdateSourceTrigger=PropertyChanged}"/>
    </Grid>
</Window>
